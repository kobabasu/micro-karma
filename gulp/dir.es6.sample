/**
 * node_modulesが存在するディレクトリからの
 * ドキュメントルートを指定
 *
 * nameはtask名の先頭になる
 */

const root = './';

export const dir = {
  name: '',

  root: root + 'test/',
  src:  root + 'test/src/',
  spec: root + 'test/spec/',
  dist: root + 'test/dist/'
}
